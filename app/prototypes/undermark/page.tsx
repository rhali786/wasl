import { UndermarkLab } from "../_components/UndermarkLab";

// UNDERMARK VARIANTS — six treatments of the status mark beneath the word.
export default function UndermarkPage() {
  return <UndermarkLab />;
}
