import { ReaderLab } from "../_components/ReaderLab";

// READER LAB — wide comparison page (not a phone frame, deliberately): pick the
// fog/status system and the definition-reveal mechanism for the real reader.
export default function ReaderLabPage() {
  return <ReaderLab />;
}
