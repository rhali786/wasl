import { ReaderRevealsDemo } from "../_components/ReaderRevealsDemo";

// READER REVEALS — the real reader in the iPhone frame; switch between the four
// non-jumping definition-reveal mechanisms on the full mushaf page.
export default function ReaderRevealsPage() {
  return <ReaderRevealsDemo />;
}
